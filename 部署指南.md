# 🚀 Karry旅行家 - 部署指南

## 📦 部署方案总览

| 方案 | 适用场景 | 成本 | 难度 | 推荐度 |
|------|---------|------|------|--------|
| **ngrok临时分享** | 快速演示、测试 | 免费 | ⭐ | ⭐⭐⭐⭐⭐ |
| **腾讯云服务器** | 长期使用、生产环境 | 10-100元/月 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Railway免费部署** | 个人使用、低流量 | 免费 | ⭐⭐ | ⭐⭐⭐⭐ |
| **Render免费部署** | 个人使用、低流量 | 免费 | ⭐⭐ | ⭐⭐⭐⭐ |
| **Vercel + Cloudflare** | 前后端分离 | 免费 | ⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 方案1：ngrok临时分享（⭐推荐用于快速演示）

### 优点
- ✅ 5分钟内完成
- ✅ 完全免费
- ✅ 无需配置服务器
- ✅ 支持HTTPS

### 缺点
- ❌ URL每次重启会变
- ❌ 需要保持本地电脑运行
- ❌ 有访问量限制（免费版）

### 步骤

#### 1. 安装ngrok
```bash
# macOS
brew install ngrok

# 或下载安装
# https://ngrok.com/download
```

#### 2. 注册账号
访问 https://ngrok.com 注册免费账号

#### 3. 配置authtoken
```bash
ngrok authtoken YOUR_AUTH_TOKEN
# 从 ngrok dashboard 获取
```

#### 4. 启动隧道
```bash
cd /Users/karryfan/.box/Workspace/output/f8b18a19-89a6-4705-9440-b757d999289f/travel-planner-doubao

# 启动服务
node backend/server.js

# 在另一个终端启动ngrok
ngrok http 3000
```

#### 5. 分享URL
ngrok会显示：
```
Forwarding  https://a1b2c3d4.ngrok-free.app -> http://localhost:3000
```

复制 `https://a1b2c3d4.ngrok-free.app` 分享给他人

---

## 方案2：腾讯云服务器部署（⭐⭐⭐⭐⭐推荐长期使用）

### 优点
- ✅ 稳定可靠
- ✅ 固定域名
- ✅ 完全控制
- ✅ 无访问限制
- ✅ 支持HTTPS

### 缺点
- ❌ 需要购买服务器（10-100元/月）
- ❌ 需要基础运维知识

### 步骤

#### 1. 购买服务器
- 推荐：腾讯云轻量应用服务器
- 配置：2核2G 或 2核4G
- 系统：Ubuntu 22.04
- 费用：约10-100元/月

#### 2. 连接服务器
```bash
ssh root@你的服务器IP
```

#### 3. 安装Node.js
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证
node -v
npm -v
```

#### 4. 上传项目
```bash
# 在本地执行
cd /Users/karryfan/.box/Workspace/output/f8b18a19-89a6-4705-9440-b757d999289f/travel-planner-doubao
tar -czf travel-planner.tar.gz --exclude node_modules .
scp travel-planner.tar.gz root@服务器IP:/root/
```

#### 5. 在服务器上部署
```bash
# 在服务器上执行
cd /root
tar -xzf travel-planner.tar.gz
cd travel-planner-doubao

# 安装依赖
npm install

# 配置环境变量
nano .env
```

#### 6. 配置.env文件
```env
# 豆包API配置
DOUBAO_API_KEY=你的API_KEY
DOUBAO_ENDPOINT_ID=你的ENDPOINT_ID

# 服务器配置
PORT=3000
NODE_ENV=production
```

#### 7. 使用PM2守护进程
```bash
# 安装PM2
npm install -g pm2

# 启动服务
pm2 start backend/server.js --name travel-planner

# 设置开机自启
pm2 startup
pm2 save
```

#### 8. 配置Nginx（可选）
```bash
# 安装Nginx
sudo apt-get install nginx

# 配置
sudo nano /etc/nginx/sites-available/travel-planner
```

Nginx配置：
```nginx
server {
    listen 80;
    server_name 你的域名.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
# 启用配置
sudo ln -s /etc/nginx/sites-available/travel-planner /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 9. 配置HTTPS（推荐）
```bash
# 安装Certbot
sudo apt-get install certbot python3-certbot-nginx

# 自动配置HTTPS
sudo certbot --nginx -d 你的域名.com
```

#### 10. 访问
浏览器访问：`http://你的服务器IP` 或 `https://你的域名.com`

---

## 方案3：Railway免费部署（⭐⭐⭐⭐推荐新手）

### 优点
- ✅ 完全免费（每月500小时）
- ✅ 自动HTTPS
- ✅ 自动部署
- ✅ 无需运维

### 缺点
- ❌ 有冷启动延迟
- ❌ 有流量限制
- ❌ 需要GitHub账号

### 步骤

#### 1. 准备代码
```bash
cd /Users/karryfan/.box/Workspace/output/f8b18a19-89a6-4705-9440-b757d999289f/travel-planner-doubao

# 初始化git（如果没有）
git init
git add .
git commit -m "Initial commit"
```

#### 2. 推送到GitHub
```bash
# 创建GitHub仓库后
git remote add origin https://github.com/你的用户名/travel-planner.git
git branch -M main
git push -u origin main
```

#### 3. 部署到Railway
1. 访问 https://railway.app
2. 使用GitHub账号登录
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择你的仓库
5. Railway会自动识别Node.js项目

#### 4. 配置环境变量
在Railway项目设置中添加：
```env
DOUBAO_API_KEY=你的API_KEY
DOUBAO_ENDPOINT_ID=你的ENDPOINT_ID
PORT=3000
```

#### 5. 启动
Railway会自动部署，几分钟后获得：
```
https://travel-planner-production.up.railway.app
```

---

## 方案4：Render免费部署（⭐⭐⭐⭐）

### 优点
- ✅ 完全免费（750小时/月）
- ✅ 自动HTTPS
- ✅ 简单易用

### 缺点
- ❌ 有冷启动延迟
- ❌ 免费版有流量限制

### 步骤

#### 1. 推送代码到GitHub（同Railway）

#### 2. 部署到Render
1. 访问 https://render.com
2. 注册账号
3. 点击 "New +" → "Web Service"
4. 连接GitHub仓库
5. 配置：
   - **Build Command**: `npm install`
   - **Start Command**: `node backend/server.js`
6. 添加环境变量：
   ```env
   DOUBAO_API_KEY=你的API_KEY
   DOUBAO_ENDPOINT_ID=你的ENDPOINT_ID
   PORT=3000
   ```

#### 3. 部署完成
获得URL：`https://travel-planner.onrender.com`

---

## 🛠️ 部署前准备

### 1. 确认环境变量
创建 `.env.example` 文件作为模板：
```env
# 豆包API配置（必需）
DOUBAO_API_KEY=your_api_key_here
DOUBAO_ENDPOINT_ID=your_endpoint_id_here

# 服务器配置（可选）
PORT=3000
NODE_ENV=production
```

### 2. 修改后端代码（支持公网访问）
确保 `backend/server.js` 中：
```javascript
const PORT = process.env.PORT || 3000;
app.listen(PORT, '0.0.0.0', () => {  // 使用 0.0.0.0 而不是 localhost
    console.log(`Server running on port ${PORT}`);
});
```

### 3. 添加健康检查端点（已有）
```javascript
app.get('/health', (req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
});
```

---

## 🔐 安全建议

### 1. 保护API密钥
- ✅ 永远不要把 `.env` 文件提交到Git
- ✅ 使用 `.gitignore` 排除敏感文件
- ✅ 在部署平台的环境变量中配置

### 2. 限制访问（可选）
```javascript
// 添加简单的访问限制
const ALLOWED_ORIGINS = [
    'https://your-domain.com',
    'http://localhost:3000'
];

app.use(cors({
    origin: function(origin, callback) {
        if (!origin || ALLOWED_ORIGINS.includes(origin)) {
            callback(null, true);
        } else {
            callback(new Error('Not allowed'));
        }
    }
}));
```

### 3. 使用HTTPS
- 生产环境必须使用HTTPS
- 免费方案（Railway/Render）自动提供
- 自建服务器使用Let's Encrypt

---

## 📊 成本对比

| 方案 | 月成本 | 流量 | 冷启动 | 稳定性 |
|------|--------|------|--------|--------|
| ngrok | ¥0 | 有限 | 无 | 中（需本地运行） |
| 腾讯云轻量 | ¥10-50 | 无限 | 无 | 高 |
| Railway | ¥0 | 有限 | 有（30秒） | 中 |
| Render | ¥0 | 有限 | 有（30秒） | 中 |

---

## 🎯 推荐选择

### 快速演示（今天就能用）
→ **ngrok临时分享**

### 个人长期使用
→ **腾讯云轻量服务器** 或 **Railway免费部署**

### 团队协作/生产环境
→ **腾讯云服务器 + 域名 + HTTPS**

### 完全免费（流量小）
→ **Railway** 或 **Render**

---

## 🆘 常见问题

### Q1: ngrok链接无法访问？
A: 检查本地服务是否运行，ngrok是否正常启动

### Q2: Railway部署失败？
A: 检查 `.env` 环境变量是否正确配置

### Q3: 如何更换域名？
A: 在Railway/Render项目设置中添加自定义域名

### Q4: 如何监控访问量？
A: Railway/Render提供访问日志和监控面板

### Q5: 如何添加用户认证？
A: 需要开发登录系统，使用JWT或OAuth

---

## 📞 技术支持

部署过程中遇到问题：
1. 查看部署平台文档
2. 检查服务日志：`pm2 logs travel-planner`
3. 检查网络连接和防火墙设置

---

**Karry旅行家** - 智能定制您的旅程 ✈️
