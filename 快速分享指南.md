# 🚀 快速分享给朋友使用

## ⭐ 最快方案：ngrok临时分享（5分钟搞定）

### 步骤

#### 1. 确保服务运行
```bash
cd /Users/karryfan/.box/Workspace/output/f8b18a19-89a6-4705-9440-b757d999289f/travel-planner-doubao
node backend/server.js
```

#### 2. 在新终端启动ngrok
```bash
# 如果没有安装ngrok，先安装：
# macOS: brew install ngrok/ngrok/ngrok

# 启动ngrok
ngrok http 3000
```

#### 3. 分享URL
ngrok会显示：
```
Forwarding  https://a1b2c3d4.ngrok-free.app -> http://localhost:3000
```

复制 `https://a1b2c3d4.ngrok-free.app` 这个链接发送给朋友即可！

---

## ⭐⭐⭐ 推荐方案：Railway免费部署（永久使用）

### 步骤

#### 1. 创建GitHub仓库
访问 https://github.com/new 创建新仓库（可以私有）

#### 2. 推送代码
```bash
cd /Users/karryfan/.box/Workspace/output/f8b18a19-89a6-4705-9440-b757d999289f/travel-planner-doubao

# 初始化git
git init
git add .
git commit -m "Initial commit"

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/travel-planner.git
git branch -M main
git push -u origin main
```

#### 3. 部署到Railway
1. 访问 https://railway.app
2. 点击 "Sign up" 使用GitHub登录
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择刚创建的仓库
5. Railway会自动识别Node.js项目

#### 4. 配置环境变量
在Railway项目页面：
1. 点击 "Variables" 标签
2. 添加以下变量：
   - `DOUBAO_API_KEY` = 你的豆包API密钥
   - `DOUBAO_ENDPOINT_ID` = 你的豆包端点ID
3. 点击 "Add Variable"

#### 5. 获取访问地址
Railway部署完成后（约3-5分钟），会显示：
```
https://travel-planner-production.up.railway.app
```

复制这个链接分享给朋友！

---

## ⭐⭐⭐⭐ 稳定方案：腾讯云服务器

### 步骤

#### 1. 购买服务器
访问 https://cloud.tencent.com/product/lighthouse 购买：
- 配置：2核2G
- 系统：Ubuntu 22.04
- 费用：约10元/月

#### 2. 连接服务器
```bash
ssh root@你的服务器公网IP
```

#### 3. 安装Node.js
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### 4. 上传项目
在本地执行：
```bash
cd /Users/karryfan/.box/Workspace/output/f8b18a19-89a6-4705-9440-b757d999289f/travel-planner-doubao
tar -czf travel-planner.tar.gz --exclude node_modules .
scp travel-planner.tar.gz root@你的服务器IP:/root/
```

#### 5. 部署
在服务器执行：
```bash
cd /root
tar -xzf travel-planner.tar.gz
cd travel-planner-doubao

# 安装依赖
npm install

# 配置环境变量
cat > .env << EOF
DOUBAO_API_KEY=你的API_KEY
DOUBAO_ENDPOINT_ID=你的ENDPOINT_ID
PORT=3000
NODE_ENV=production
EOF

# 安装PM2
npm install -g pm2

# 启动服务
pm2 start backend/server.js --name travel-planner
pm2 save
pm2 startup
```

#### 6. 访问
浏览器访问：`http://你的服务器IP`

---

## 📱 移动端访问

所有部署方案都支持移动端访问，朋友可以在手机浏览器直接打开链接使用。

---

## 🔒 关于API密钥

**重要提醒**：
1. 部署到公网时，任何人都可以访问你的应用
2. 这意味着他们会使用你的豆包API额度
3. 如果担心费用，可以：
   - 设置访问密码（需要开发）
   - 使用付费版本，API额度更高
   - 让朋友自己申请API密钥

---

## 💡 快速对比

| 方案 | 时间 | 成本 | 稳定性 | 适用场景 |
|------|------|------|--------|---------|
| **ngrok** | 5分钟 | 免费 | ⭐⭐⭐ | 临时演示 |
| **Railway** | 15分钟 | 免费 | ⭐⭐⭐⭐ | 个人长期使用 |
| **腾讯云** | 30分钟 | 10元/月 | ⭐⭐⭐⭐⭐ | 团队/生产 |

---

## 🎯 推荐选择

### 想今天就能分享给朋友
→ **ngrok**（最快）

### 想长期免费使用
→ **Railway**（推荐）

### 想稳定可靠
→ **腾讯云**（最稳定）

---

## 🆘 需要帮助？

如果遇到问题，查看详细文档：[部署指南.md](./部署指南.md)

---

**Karry旅行家** - 智能定制您的旅程 ✈️
